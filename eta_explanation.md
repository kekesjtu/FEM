# eta 在二维有限元中的含义

## 概述

在二维有限元方法中，`eta` 是**参考单元**（标准单元）中的第二个局部坐标。与 `xi` 一起，它们构成了二维参考坐标系统 (ξ, η)。

## 参考三角形单元

在标准的线性三角形单元中：

```
标准参考三角形的三个顶点：
- 节点 0: (ξ=0, η=0)  ← 原点
- 节点 1: (ξ=1, η=0)  ← 沿ξ轴
- 节点 2: (ξ=0, η=1)  ← 沿η轴

约束条件：ξ ≥ 0, η ≥ 0, ξ + η ≤ 1
```

## eta 的几何意义

1. **坐标轴方向**：
   - `xi` 是参考坐标系的第一个坐标轴（类似x轴）
   - `eta` 是参考坐标系的第二个坐标轴（类似y轴）

2. **重心坐标关系**：
   - 在三角形内任意一点，重心坐标为 (1-ξ-η, ξ, η)
   - `eta` 表示该点相对于节点2的"权重"

## 形函数中的 eta

在线性三角形单元中，三个形函数为：

```cpp
N₀(ξ, η) = 1 - ξ - η    // 节点0的形函数
N₁(ξ, η) = ξ            // 节点1的形函数  
N₂(ξ, η) = η            // 节点2的形函数 ← eta直接作为形函数
```

**关键特性**：
- 当 `eta = 1` 且 `xi = 0` 时，只有N₂=1，其他形函数为0
- 当 `eta = 0` 时，N₂=0，点在底边上
- `eta` 的值表示点到底边的"距离"（归一化）

## 偏导数中的 eta

形函数对 `eta` 的偏导数：

```cpp
∂N₀/∂η = -1    // 常数
∂N₁/∂η =  0    // N₁与η无关
∂N₂/∂η =  1    // 线性关系
```

## 几何映射中的 eta

从参考单元到物理单元的映射：

```cpp
x(ξ, η) = x₀·(1-ξ-η) + x₁·ξ + x₂·η
y(ξ, η) = y₀·(1-ξ-η) + y₁·ξ + y₂·η
```

**eta 的作用**：
- 控制映射在 y 方向的变化
- `∂x/∂η = x₂ - x₀` 和 `∂y/∂η = y₂ - y₀`

## 数值积分中的 eta

在高斯积分中，积分点用 (ξᵢ, ηᵢ) 表示：

```cpp
∫∫ f(ξ,η) dξdη ≈ Σᵢ wᵢ·f(ξᵢ, ηᵢ)
```

## 物理意义总结

1. **局部坐标**：eta 是三角形单元内的标准化局部坐标
2. **插值权重**：eta 值决定了节点2对插值的贡献程度
3. **几何变换**：eta 参与从标准单元到实际单元的坐标变换
4. **求导计算**：eta 方向的偏导数用于计算梯度和刚度矩阵

## 与一维情况的对比

| 维度 | 参考坐标 | 节点数 | 形函数 |
|------|----------|--------|--------|
| 1D   | ξ        | 2      | N₀=½(1-ξ), N₁=½(1+ξ) |
| 2D   | (ξ, η)   | 3      | N₀=1-ξ-η, N₁=ξ, N₂=η |

在二维情况下，增加了 `eta` 坐标来描述第二个维度的变化。
